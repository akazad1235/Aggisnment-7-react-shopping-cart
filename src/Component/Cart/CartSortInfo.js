import React from 'react';

const CartSortInfo = (props) => {
   // const{name, price, img} = props
   const name = props.name;
   
    return (
        <div>
            <p></p>
        </div>
    );
};

export default CartSortInfo;